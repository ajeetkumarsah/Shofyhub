import 'package:flutter/material.dart';

class DeleteShopUser extends StatelessWidget {
  const DeleteShopUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
