import 'package:flutter/material.dart';

class EditShopUser extends StatelessWidget {
  const EditShopUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
